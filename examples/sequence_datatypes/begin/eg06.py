import sys
# The first argument is always the name of the script
print(f'Script name: {sys.argv[0]}')

# The rest of the arguments are the command line arguments
print(f'Other arguments: {sys.argv[1:]}')

#do magic things with the values passed in
